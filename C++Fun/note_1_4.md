1、为什么数据竞争是未定义行为，而不是"结果可能不对"？
A:编译器优化代码时，尤其是线程函数，会默认没有数据竞争。

2、为什么单核CPU也会出现数据竞争的情况?
A:并发的概念，一段时间内几个程序同时执行，可以在单核也可以多核，宏观上并行，微观上交替顺序执行。
  多个程序在单核CPU上并发执行时，内核可以将CPU分配给其他。如自增操作(3条指令)，在执行完第一条时，可能把CPU分给另一个进程，如果操作相同的变量就出现了数据竞争。

3、为什么volatile不能解决数据竞争问题？
A:volatile的作用是告诉编译器，这个变量的值可能会被程序之外的因素修改，因此不要对这个变量做任何优化，每次都从内存中读取，不要缓存到寄存器中。
  volatile为了访问内存映射的硬件寄存器设计的。volatile uint32_t* timer_reg = (volatile uint32_t*)0x4000000;
  volatile不能解决数据竞争的问题，正确的方法应该是原子类型和内存序。