1、为什么条件变量需要和互斥锁一起使用？如果没有互斥锁会怎么样？
A: 条件变量负责休眠和唤醒，当线程被唤醒时，wait()会重新获得锁，然后才返回。
   其中lock_guard没有unlock()和lock()的接口，所以无法自主释放和获取，必须用unique_lock

2、为什么必须用while循环检查条件，而不能用if?
A: 虚假唤醒。条件变量的wait没有被notify或者notify_all主动唤醒，但是自己从wait中醒过来。
   内核为了容错，允许无条件唤醒一次条件变量。/ 信号处理或中断导致的唤醒。 / 多处理器边界处理的唤醒。
   当发生虚假唤醒的时候，wait()会返回，但是队列仍然是空的。

3、为什么notify不会立即唤醒线程?
A: notify只是告诉内核，这个线程可以被唤醒。加入到队列中，内核会根据自己的调度策略唤醒线程。

4、为什么有界队列需要用两个条件变量来控制？
A: 因为notify_one有可能唤醒的还是生产者，然后再度休眠，导致额外的等待效果。

5、如何实现带超时机制的pop方法？
A: 使用wait_for方法，三个参数分别是Lock锁、RealTime 指定时间的chrono::duration对象、Pred返回值为false或true的表达式