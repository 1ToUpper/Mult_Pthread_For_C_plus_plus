# 面试为问题

## 1、设计目标：你这个多线程实时日志库的主要设计目标是什么？（例如：实时性、持久性、吞吐量、低延迟、可扩展性、资源约束等）并说明你是如何在这些目标间做权衡的。

### 由于工业控制系统、网络多并发后端中常常使用日志打印有效信息帮助定位问题，所以设计一个线程池可以自动回收线程的日志库，线程池符合C++中RAII的设计思想，自动回收线程资源。 

### 指标测试方法：埋点测试
使用原子变量类型，在日志消息存入队列时、队列消息实际写入文件时，记录数量、字节数可以计算出指标

### 【指标】吞吐量
- 32 个 producer，消息大小 128 字节，持续 10s
- 平均实际写入（written）≈ 10,875 msg/s
- 平均吞吐 ≈ 1.69 MB/s
- 峰值写入 ≈ 21,302 msg/s
- 峰值提交（submit）观察到 ≈ 67,909 msg/s
- 丢弃（dropped）= 0（未发生丢弃）

- 64 个 producer，消息大小 256 字节，持续 60s
峰值提交（submitted）: 217,133 msg/s
峰值写入（written）: 27,968 msg/s
峰值吞吐: 7.76166 MB/s
样本平均写入（12s 样本）: ~18,881 msg/s
样本平均吞吐: ~5.24 MB/s
丢弃（dropped）: 0（未发生丢弃）

### 如果生产者写入日志的速度远超过写入磁盘的速度怎么优化？
#### 应用层面
1、多个日志合并成一条
2、增加每次写入大小或者减少触发频率
3、扩大缓冲区大小
#### 流量控制
1、采用BLOCK阻塞等待的方式，控制生产者的速度，使其与写入磁盘速度接近
2、调整打印数据优先级（ERROR/INFO/WARNING）仅输出有效日志
#### 存储IO层面
1、优化文件的读写